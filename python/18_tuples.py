t = (1, 2, 4, 5,) #tuple with more than 1 elements
# print(t[0])
''' we cannot updates the value in tuples, that 1 reason
who create difference between list and tuples '''
# t1 = () Empty tuple
# t1 = (1,) tuple with single element
# to write tupel with single element , should be added 
# print(t1)
print(t.count(1))
# .count help to count how many time a tuple is repeated
print(t.index(4))
# .index help to search where a tuple lies